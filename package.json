{
  "name": "catppuccin-starlight",
  "version": "0.0.1",
  "private": "true",
  "scripts": {
    "dev": "concurrently \"pnpm --filter docs dev\" \"pnpm --filter @catppuccin/starlight dev\"",
    "build": "pnpm --filter @catppuccin/starlight --filter docs build"
  },
  "devDependencies": {
    "astro-auto-import": "^0.4.4",
    "concurrently": "^9.0.1"
  },
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "astro": "^5.12.8",
    "astro-auto-import": "^0.4.4"
  }
}
